import React from 'react';
import Form from '../components/Form';

const Register = () => {
    return (
        <div>
            <Form method="Register" />
        </div>
    );
};

export default Register;